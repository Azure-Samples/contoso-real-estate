{
  "name": "contoso-real-estate",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "start": "swa start",
    "test": "npm run test -ws",
    "format": "npm run prettier -- --write",
    "format:check": "npm run prettier -- --check",
    "lint": "npm run format:check",
    "compile": "tsc -b tsconfig.build.json",
    "clean": "rimraf \"packages/**/*.tsbuildinfo\"",
    "postinstall:portal": "npm install --prefix packages/portal",
    "postinstall:api": "npm install --prefix packages/api",
    "postinstall:website": "npm install --prefix packages/website",
    "postinstall": "npm run postinstall:portal && npm run postinstall:api && npm run postinstall:website"
  },
  "keywords": [],
  "author": "Microsoft",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=16",
    "npm": ">=7"
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^1.0.3",
    "prettier": "^2.7.1",
    "typescript": "^4.8.2"
  }
}
